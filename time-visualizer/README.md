# 時間小精靈

一個給孩子使用的視覺化倒數計時器。它用色塊、秒圈、角色動畫和音效提示時間流逝，並提供兒童鎖與家長數學驗證。

## 功能

- 視覺化倒數：一小時內用圓形色塊呈現，超過一小時會加外圈。
- 精準計時：用目標結束時間校正剩餘秒數，減少瀏覽器卡頓造成的時間漂移。
- 動物接棒：倒數中每跑一分鐘會換一隻隨機動物。
- 隨機驚喜：每跑完一圈有機率出現貓貓雨、象群遷徙或星星爆發等短動畫。
- 本地 SVG 視覺：背景、倒數角色、閒置時鐘與完成動畫使用本地 OpenMoji SVG，不依賴外部 CDN。
- 時間提醒：15 分鐘、5 分鐘、3 分鐘、1 分鐘和結束時播放不同音效。
- 兒童鎖：鎖定後隱藏控制按鈕，解鎖需要完成乘法題。
- 閒置時鐘：未倒數且閒置一段時間後切換成時鐘模式。
- 狀態快取：關閉瀏覽器後重新開啟，會從本機快取還原倒數狀態。
- 快速參數：網址可加 `?min=25` 直接設定 25 分鐘。

## 開發

```bash
npm install
npm run dev
```

## 驗證

```bash
npm run lint
npm run build
```

倒數分鐘會限制在 1 到 180 分鐘之間。

## 素材授權

`src/assets/openmoji` 內的 SVG 來自 OpenMoji，採 CC BY-SA 4.0 授權。
